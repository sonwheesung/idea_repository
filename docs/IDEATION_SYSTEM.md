# IDEATION_SYSTEM — 발상 도구(Idea Lab)

> 아이디어가 **아직 없거나 막연할 때** 아이디어를 만들어내는 도구. 앱의 범위를 Capture 앞 단계(**Ideate**)로 한 칸 넓힌다.
> 개요·결정은 [`../CLAUDE.md`](../CLAUDE.md) §3 #22·§14 N, 프로젝트 필드는 [`PROJECT_SYSTEM.md`](./PROJECT_SYSTEM.md), 스키마는 [`DATABASE.md`](./DATABASE.md) v2.
> 화면 시안: 아티팩트 "Idea Lab 화면 흐름"(2026-08-18, 한국어판) — 사용자 승인("괜찮네 그걸로 진행해보자").
> 작성 2026-08-18(문서 선행 — 코드 착수 전).

## 구현 현황

| 영역 | 상태 |
|---|---|
| 문서(이 문서 · CLAUDE §3/§5/§14 · DATABASE v2 · PROJECT_SYSTEM · I18N) | ✅ 2026-08-18 |
| DB v2 `projects.approach` + 폼 Select + 상세 표시 | ✅ 2026-08-18 — 기존 v1 DB 마이그레이션 에뮬 실측 |
| 단어 풀·문장 틀(ko/en) `features/ideation/pool.ts` | ✅ 2026-08-18 — 15그룹×12 + 만약에 40 (언어별) · `shuffle.ts` |
| 화면 5종(입구·도구 목록·조합·개선·불편에서·만약에) + 프로젝트 저장(미리 채움) | ✅ 2026-08-18 — `app/idea-lab/*` · `features/ideation/prefill.ts` · `/project/new` 파라미터 |
| 에뮬레이터 실측 | ✅ 2026-08-18 조합(섞기·단어 시트·직접 입력·저장→태그·핵심 아이디어·approach) · 개선(5문·답 2개→노트 2개) · 미리 채움 펼침. ⏳ 불편에서·만약에 화면은 같은 부품(QuestionCard·StepDots)이라 미실측 — 다음 세션에서 |

## 1. 배경·결정 (2026-08-18 사용자)

- 사용자 원문: "카테고리는 필요하지 … 그 이전에 **아이디어를 만드는 과정에 필요한 툴**을 제공하고 싶다" · "스포츠와 육아를 조합해보고 있는 걸 기능 추가나 수정도 생각해보고".
- 확정: **분류 필드가 아니라 도구**다. 결과가 마음에 들면 **한 탭에 프로젝트로** 저장된다(기둥 1 Fast Capture와 연결).
- 무작위 = **서버·AI 없이 앱 내장 단어·문장 목록에서 뽑는다**(사용자 승인 "괜찮네 그걸로"). 기둥 2·3 유지 — 어떤 입력도 기기를 떠나지 않는다.
- 발상 도구 화면에는 **광고 없음**(작성 흐름 — CLAUDE.md §7 기둥 5).
- 첫 버전 도구 4종 전부(질문 카드 엔진이 공용이라 도구 추가 비용 = 리소스뿐). 콘텐츠는 소박하게 시작해 업데이트로 키운다.
- 시점: 코드는 main에 들어가고, **다음 AAB(vc2)에 포함할지는 사용자 결정**(비공개 테스트 중 — CLAUDE.md §16 OTA 금지 규칙과 별개).

## 2. 입구

| 위치 | 동작 |
|---|---|
| 메인 헤더 — [＋] 왼쪽 **전구 아이콘**(항상) | `/idea-lab` |
| 메인 빈 화면 — "아직 막연하다면? **발상 도구 열기 →**" 링크 | `/idea-lab` |

[＋] 탭 시 "빈 프로젝트 / 발상 도구" 선택 시트 안은 🚫 — 빠른 기록에 탭이 하나 늘어난다.

## 3. 화면

```
/idea-lab            도구 목록 — 설정 화면과 같은 행 문법(아이콘 · 제목 · 한 줄 설명). SettingRow 재사용 금지(도메인 다름) → IdeaLabRow
/idea-lab/combine    조합
/idea-lab/improve    개선
/idea-lab/problem    불편에서
/idea-lab/whatif     만약에…
```

모두 스택 화면(제목 = 도구 이름). 저장은 `/project/new`로 이동하며 **미리 채움 파라미터**를 넘긴다(§6).

### 3.1 조합(Combine)

- 슬롯 A × 슬롯 B. 슬롯 탭 → **단어 선택 시트**(검색 입력 + 목록 + 입력한 그대로 쓰기).
- 버튼: **둘 다 섞기** · **오른쪽만 섞기**(왼쪽 고정 — 스포츠를 잡아두고 상대만 돌리기).
- **단어 출처** Select: `내장 단어` / `내장 + 내 태그·카테고리` / `내 태그·카테고리만`. 내 것이 2개 미만이면 내장으로 자동 보충.
- 결과 카드 "A × B — 무엇이 될 수 있을까?" + **떠오르는 것** 한 줄(선택).
- 저장: 이름 = `A × B`, 핵심 아이디어 = 떠오르는 것, 태그 = [A, B], approach = combine.

### 3.2 개선(Improve) — SCAMPER 축약

- 입력 "이미 있는 것"(제품·서비스명, 필수 — 없으면 질문을 만들 수 없다).
- 질문 카드 5장(더하기 · 빼기 · 뒤집기 · 다른 용도 · 다른 대상), 각 답은 선택. 이전/다음, 점 인디케이터.
- 저장: 이름 = `{X} 개선` / `Improve {X}`, 노트 = 답이 있는 카드마다 "질문 — 답" 1개, approach = improve.

### 3.3 불편에서(Problem)

- 질문 카드 3장: 오늘 뭐가 불편했나 · 누가 같은 불편을 겪나 · 지금은 어떻게 버티나.
- 저장: 이름 = 첫 답의 앞 40자, 문제점 = 첫 답, 타겟 사용자 = 둘째 답, 노트 = 셋째 답, approach = problem.

### 3.4 만약에…(What if)

- 문장 틀 × 단어 풀 → 카드 1장씩("**{X}**를 아이용으로 만들면?"). **다음 카드** / 마음에 들면 떠오르는 것 한 줄 + 저장.
- 저장: 이름 = 카드 문장, 핵심 아이디어 = 떠오르는 것, 태그 = [X], approach = whatif.

## 4. 단어 풀·문장 틀 (정본 = `features/ideation/pool.ts`)

- 언어별(ko/en) **앱 리소스** — 번역이 아니라 각 언어에 자연스러운 단어를 따로 쓴다. 사용자 콘텐츠 자동 번역 금지 원칙과 무관(앱 리소스).
- 영역(그룹) 15개 × 10~14개 ≈ 180단어/언어: 일상 · 건강 · 육아 · 스포츠 · 음식 · 여행 · 교육 · 돈 · 일/업무 · 취미 · 기술 · 환경 · 반려동물 · 게임 · 동네/지역.
- 만약에… 문장 틀 ≈ 40개(`{X}` 슬롯 하나). 개선 질문 5 · 불편 질문 3은 i18n 키(`ideation.improve.q.*` 등).
- 단어 풀은 `check:i18n` 대상이 아니다(JSON 리소스가 아니라 TS 데이터). 두 언어의 **그룹 수·순서는 같아야** 한다(코드에서 assert).

## 5. 섞기 규칙 (`features/ideation/shuffle.ts`)

1. 두 슬롯은 **서로 다른 그룹**에서 뽑는다(축구 × 야구 방지).
2. 최근 보여준 **20쌍은 피한다**(모듈 메모리, 순서 무관 비교). 20번 시도 후에도 못 피하면 그냥 낸다.
3. 출처가 "내 태그·카테고리"를 포함하면 그 단어들은 가상 그룹 `mine`으로 합류(자기들끼리도 조합 가능 — 사용자 것은 영역을 모른다).
4. `Math.random` — 재현성 불필요, 저장 안 함.

## 6. 프로젝트 저장 — 미리 채움 파라미터

`/project/new`가 `useLocalSearchParams`로 읽는 선택 파라미터(전부 문자열):

| 파라미터 | 매핑 |
|---|---|
| `name` | 프로젝트명 |
| `coreIdea` · `problem` · `targetUser` | 같은 필드 |
| `approach` | `combine \| improve \| problem \| whatif` |
| `tags` | JSON 배열 문자열 |
| `notes` | JSON 배열 문자열 — 저장 직후 `createNote`로 순서대로 추가 |

생성 폼은 미리 채워진 값이 있으면 **"Add details" 펼침 상태로 시작**한다(무엇이 채워졌는지 보이게). 저장 흐름·검증은 기존과 동일.

## 7. `approach` 필드 (PROJECT_SYSTEM §3.3 보강)

- 값 `combine · improve · problem · whatif · other · none`(기본 `none`). 도구와 1:1 + 수동 `other`.
- 폼(생성·편집) "Idea" 섹션에 Select **발상 방식**. 상세 화면 Idea 섹션에 값이 none이 아니면 표시. 카드·필터·검색에는 넣지 않는다(필요해지면 결정).
- DB: v2 `ALTER TABLE projects ADD COLUMN approach TEXT NOT NULL DEFAULT 'none' CHECK(...)`.

## 8. i18n 키

`ideation.*`(도구 이름·설명·버튼·질문) · `approach.*`(6값 표시명) · `home.ideaLab`(빈 화면 링크) · `settings` 없음. 상세는 [`I18N_SYSTEM.md`](./I18N_SYSTEM.md).

## 9. 제외 (지금 안 한다)

AI 생성 · 서버 추천 · 다른 사용자 데이터 · 사용자 정의 단어 풀 편집 화면(직접 입력은 가능) · 발상 이력 저장(프로젝트로 저장한 것만 남는다) · 카드/필터에 approach 노출.
